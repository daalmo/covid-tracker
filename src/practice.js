// const randomArray = (min, max) => {
    
//     return myArray
// }

const squareRootsArray = array => {
    array.map( math.sqrt );
    return array;
}

exArray = [ 1, 5, 16, 24 ];

console.log( squareRootsArray( exArray ));

daisyObject = {
    firstName: 'Daisy',
    lastName: 'Mosqueda',
    family: {
        sister: 'Ari',
        brother: 'Alexis'
    }
}